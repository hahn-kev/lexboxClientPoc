{
  "name": "lexbox-razor",
  "scripts": {
    "build": "npm run build:Release",
    "build:Release": "npm run build --workspaces && webpack --mode production",
    "build:Debug": "npm run build --workspaces && webpack --mode development",
    "watch": "webpack --watch --mode development"
  },
  "devDependencies": {
    "ts-loader": "^9.2.6",
    "typescript": "^4.5.4",
    "webpack": "^5.72.1",
    "webpack-cli": "^4.9.2"
  },
  "workspaces": ["../SvelteComponents"]
}
